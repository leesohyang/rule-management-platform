ktor {
    deployment {
        port = 8096
        port = ${?PORT}
    }
    application {
        modules = [ com.sample.ApplicationKt.module ]
    }

    hikariconfig = "resources/dbconfig.properties"
}
